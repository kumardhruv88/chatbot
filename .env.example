# Backend Environment Variables (for Railway)
# =============================================

# API Keys (REQUIRED)
GROQ_API_KEY=your_groq_api_key_here
TAVILY_API_KEY=your_tavily_api_key_here
POLLINATIONS_API_KEY=your_pollinations_key_here  # Optional

# Frontend URL for CORS (REQUIRED for deployment)
# Set this to your Vercel frontend URL
FRONTEND_URL=https://your-frontend.vercel.app

# Server Configuration (optional, uses defaults)
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Database (auto-configured, no need to set)
# DATABASE_URL=sqlite:///./chatbot.db

# FAISS Vector Database directory (auto-configured)
# FAISS_PERSIST_DIR=/tmp/faiss_db

# Upload directory (auto-configured)
# UPLOAD_DIR=/tmp/uploads
